part of dot_stepper;

enum Shape {
  circle,
  square,
  rectangle,
  stadium,
  squircle,
  dash,
}

enum Indicator {
  shift,
  slide,
  worm,
}

enum IndicatorType {
  filled,
  outlined,
  squashed,
}
