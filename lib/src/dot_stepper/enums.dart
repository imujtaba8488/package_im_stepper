part of dot_stepper;

enum Shape {
  circle,
  square,
  rectangle,
  rounded_rectangle,
  rounded_square,
  dash,
}

enum Indicator {
  shift,
  slide,
  worm,
}

enum IndicatorType {
  filled,
  outlined,
  squashed,
}
